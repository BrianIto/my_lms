version: "2"
sql:
  - engine: postgresql
    queries: internal/db/query
    schema: internal/db/migrations
    gen:
      go:
        package: repository
        out: internal/repository
        emit_interface: true
        emit_json_tags: true
        sql_package: pgx/v5
