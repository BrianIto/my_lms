**What needs to be done**:
Move admin course authoring from create/update-only to supporting safe DELETE actions for courses, modules, lessons, and individual lesson bookmarks, with backend admin endpoints, cache invalidation, tests/docs, and frontend confirmation UI.

**Risk level**: high
This introduces destructive operations across persisted LMS catalog records, shared API contracts, cache invalidation behavior, admin authorization, and frontend mutations.

**Stack & tooling**:
- Language/Framework: Go backend with chi + PostgreSQL/pgx; TypeScript + React 19 + TanStack Start/Router/Query frontend
- Test runner: Go test; Vitest/Biome for frontend checks
- Verification command: `cd backend-go && make test && make docs && cd ../frontend-react && bunx --bun biome check src/routes/_protected.admin.tsx src/lib/backend-api.ts && bun --bun run build`
- If unsure about versions, check `backend-go/go.mod` and `frontend-react/package.json`

---

**Context to read first** (do this before writing any code):
- `AGENTS.md` — service boundaries, admin/auth rules, cache rules, and repository commands
- `plan/README.md` — plan index and cross-cutting LMS constraints
- `plan/02-course-catalog-and-cache.md` — normalized course/module/lesson/bookmark model and required cache invalidation strategy
- `plan/05-admin-and-production.md` — admin authoring route expectations and production/security rules
- `design.md` — frontend admin UI must keep the dark, border-first, quietly premium LMS cockpit style
- `backend-go/internal/db/migrations/004_course_catalog.up.sql` — confirms FK cascade behavior for course→module→lesson→bookmark deletion
- `backend-go/internal/handler/handler.go` — existing admin route registration and middleware placement
- `backend-go/internal/handler/admin.go` — current create/update admin handlers, Swagger annotations, and service error mapping
- `backend-go/internal/service/course.go` — current authoring service methods and cache invalidation helpers
- `backend-go/internal/repository/course.go` — current repository queries and transaction patterns
- `backend-go/internal/service/course_test.go` — fake repo/cache patterns and service-level cache invalidation tests
- `backend-go/internal/handler/admin_test.go` — admin middleware and admin endpoint coverage
- `backend-go/internal/handler/course_test.go` and `backend-go/internal/repository/course_integration_test.go` — catalog/detail and DB behavior expectations
- `frontend-react/src/lib/backend-api.ts` — frontend backend API client and admin mutation helpers
- `frontend-react/src/routes/_protected.admin.tsx` — Course inventory UI, mobile action menu, outline rendering, dialogs, query invalidation, and toast patterns
- `frontend-react/src/components/ui/button.tsx` and existing dialog components — existing shadcn-style controls to reuse before adding new primitives

**Architecture context**:
The backend owns LMS catalog persistence and must enforce session + admin permission for destructive operations; frontend gating is UX only. The database already uses `ON DELETE CASCADE` for course/module/lesson/bookmark relationships, but service/repository code must still resolve parent course slugs before deletion so public/detail caches are invalidated correctly. If you discover progress/enrollment tables now reference lessons/courses without cascade or archival policy, STOP and ask before proceeding.

---

**Scope — files you ARE allowed to modify** (whitelist; leave EMPTY if using blacklist below):
- `backend-go/internal/handler/handler.go` — register DELETE admin routes under existing `requireAdmin` middleware
- `backend-go/internal/handler/admin.go` — add DELETE handlers, Swagger annotations, and error mapping usage
- `backend-go/internal/service/course.go` — add delete service methods, validation/not-found mapping, and cache invalidation
- `backend-go/internal/repository/course.go` — add delete queries and parent-slug resolution helpers as needed
- `backend-go/internal/repository/repository.go` — update repository interface if it declares course methods
- `backend-go/internal/service/course_test.go` — add fake repo methods and service tests for delete behavior/cache invalidation
- `backend-go/internal/handler/admin_test.go` — add endpoint tests for delete success, missing resources, auth/admin guard reuse, and malformed cases if applicable
- `backend-go/internal/repository/course_integration_test.go` — add DB-level cascade/delete integration tests where lightweight and consistent with existing patterns
- `backend-go/docs/docs.go`, `backend-go/docs/swagger.json`, `backend-go/docs/swagger.yaml` — regenerated by `make docs` after route annotation changes
- `frontend-react/src/lib/backend-api.ts` — add typed admin delete client functions
- `frontend-react/src/routes/_protected.admin.tsx` — add delete actions/confirmation UI for courses, modules, lessons, and bookmarks while preserving current admin layout/motion patterns
- `frontend-react/src/components/ui/*` — only if an already-installed shadcn-style primitive must be minimally adjusted; prefer composing existing `Dialog`/`Button`

---

**Confidence check** (do this before step 1):
Restate that you are implementing destructive admin deletion for the course catalog at every level: course, module, lesson, and individual bookmark/sequence point. Confirm that course/module/lesson deletes should cascade to their children according to existing DB constraints, while bookmark delete removes only that one sequence point. List any assumption you are making about response status (`204 No Content` preferred) and confirmation copy; if any assumption is shaky, ask before proceeding.

**Workflow steps**:
1. Inventory existing admin create/update routes, service methods, repository interface/query patterns, tests, frontend admin action menus, and cache invalidation behavior.
2. Design and register RESTful admin DELETE routes for courses, modules, lessons, and lesson sequence points, all under the existing `requireAdmin` middleware and documented with Swagger annotations.
3. Implement repository and service delete methods so not-found errors map cleanly, deletions use DB cascade where appropriate, parent course slug/list impact is resolved before deleting, and list/detail caches are invalidated correctly.
4. Add or update backend tests for success, missing resource, cascade behavior, admin access, and cache invalidation; regenerate OpenAPI docs.
5. Add typed frontend delete client helpers and wire delete actions into the admin Course inventory UI: course delete in the row menu/actions, module delete near each module, lesson delete near each lesson, and bookmark delete in the sequence/bookmark management flow.
6. Use explicit confirmation UI for destructive actions with clear copy about cascading child deletion, preserve accessibility/keyboard behavior, keep mobile menu behavior intact, and show success/error toasts.
7. Run the verification command and fix any issues within scope.

> Steps should be milestones, not micro-instructions. You decide the sub-steps.

**If you get stuck or discover the task is wrong**:
- Do NOT force the change through.
- Stop, summarize what you found, and ask. A wrong chore done well is worse than a right chore left undone.

---

**Do not break**:
- [ ] Existing backend tests still pass (`cd backend-go && make test`)
- [ ] Frontend focused checks/build still pass (`cd frontend-react && bunx --bun biome check src/routes/_protected.admin.tsx src/lib/backend-api.ts && bun --bun run build`)
- [ ] Type checks/build pass for touched frontend files
- [ ] Existing create/update/list/detail/progress routes keep their public signatures and behavior
- [ ] Admin delete endpoints require the same session + admin permission as existing admin writes
- [ ] Public course list still excludes drafts and never uses admin inventory data
- [ ] Cache invalidation removes affected list/detail keys after delete operations
- [ ] Course delete removes modules, lessons, and bookmarks via DB cascade; module delete removes lessons/bookmarks; lesson delete removes bookmarks; bookmark delete does not delete the lesson
- [ ] Missing IDs return 404 rather than succeeding silently
- [ ] Frontend delete actions never run without a confirmation step and invalidate relevant TanStack Query keys after success
- [ ] No auth service code, unrelated schemas, env/secret files, or generated route tree files are modified

**Done looks like**:
- Files changed: only files within scope above
- Command to verify: `cd backend-go && make test && make docs && cd ../frontend-react && bunx --bun biome check src/routes/_protected.admin.tsx src/lib/backend-api.ts && bun --bun run build`
- Expected output: backend tests pass, Swagger docs regenerate, focused frontend Biome check passes, and production build succeeds with no new errors
- Observable state: an admin can delete a course, module, lesson, or individual bookmark from Course inventory after confirmation; deleted entities disappear from admin inventory/course outline; child entities are cascaded appropriately; stale public/detail course data is not served after deletion
